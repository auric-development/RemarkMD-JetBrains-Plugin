# The IntelliJ Platform bundles its own Kotlin stdlib; don't let the Kotlin Gradle plugin add a
# second (possibly mismatched) copy. See https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency=false

# Opt in to the platform's isolated plugin classloader repository handling.
org.gradle.jvmargs=-Xmx2g
